version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist && cp index.html styles.css app.js dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
